name: Legacy ISO workflow (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  use-final-installer:
    runs-on: ubuntu-24.04
    steps:
      - name: Point to the maintained offline ISO workflow
        run: |
          echo 'This legacy workflow is retired. Run build-k1-final-iso.yml on k1.0-final-restyle-fixes.' >&2
          exit 1
