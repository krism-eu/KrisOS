[main]
# KrisOS is single-arch: no i686/multilib payloads.
excludepkgs=*.i686
multilib_policy=best
# KrisOS installs only explicitly requested dependency closure; optional weak
# dependencies must be chosen deliberately rather than pulled in implicitly.
install_weak_deps=False
